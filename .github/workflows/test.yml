
name: Remove File Test

on: workflow_dispatch

jobs:
  delete_file:
    runs-on: ubuntu-latest
    steps:
      - name: Remove File
        uses: JesseTG/rm@v1.0.0
        with:
          path: Pishing-Database/database/domains.json
      
